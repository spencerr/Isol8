apiVersion: v1
kind: Service
metadata:
  name: {{ include "isol8.fullname" . }}
  labels:
    {{- include "isol8.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isol8.selectorLabels" . | nindent 4 }}
